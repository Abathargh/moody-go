version: "3"

services:
  moody-postgres:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=password
    networks:
      - postgres
    volumes:
      - ./postgres/db_init.sql:/docker-entrypoint-initdb.d/db_init.sql
      - ./postgres/data:/var/lib/postgresql/data

  moody-mongo:
    image: mongo
    networks:
      - mongo
    volumes:
      - ./mongo/data:/data/db

  moody-api-gw:
    image: nginx:latest
    volumes:
      - ./api_gateway/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - nginx-back
    ports:
      - 80:80
    depends_on:
      - moody-neural
      - moody-dataset
      - moody-service

  moody-neural:
    image: abathargh/moody-neural
    networks:
      - nginx-back
      - mongo
    depends_on:
      - moody-mongo

  moody-dataset:
    image: abathargh/moody-dataset
    networks:
      - nginx-back
      - mongo
    depends_on:
      - moody-mongo

  moody-service:
    image: abathargh/moody-service
    networks:
      - nginx-back
      - postgres
    depends_on:
      - moody-postgres

  moody-situation:
    image: abathargh/moody-situation
    networks:
      - nginx-back
      - postgres
    depends_on:
      - moody-postgres

networks:
  nginx-back:
  postgres:
  mongo: